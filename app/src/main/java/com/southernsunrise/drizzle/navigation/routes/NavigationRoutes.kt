package com.southernsunrise.drizzle.navigation.routes

enum class NavigationRoutes(val route: String) {
    MainWeatherInfoScreen(route = "main_weather_info_screen"),
    DailyForecastScreen(route = "daily_forecast_screen")
}